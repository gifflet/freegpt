# FreeGPT Environment Variables
# Copy this file to .env and update with your values

# Path to store the GitHub Copilot token
# Default: /app/data/.copilot_token (inside container)
COPILOT_TOKEN_PATH=/app/data/.copilot_token

# Ollama host configuration for embeddings
OLLAMA_HOST=http://HOST_IP_RUNNING_OLLAMA:11434